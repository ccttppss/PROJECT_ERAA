# E-commerce Review Analysis Agent

[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**LLM 기반 Multi-Agent AI 시스템**으로 데이터 입력부터 최종 실행 계획 수립까지의 **전 과정**을 **자동화**하는 아키텍처를 구현합니다.
이를 검증하기 위해, 제조 현장보다 데이터의 노이즈가 심하고 비정형성이 높은 **이커머스 고객 리뷰(Amazon 리뷰)를 자동 분석**하여 **실행 가능한 비즈니스 인사이트**와 **구체적인 개선 계획**을 도출하는 시스템을 구축했습니다.

> ⚠️ **데이터셋 다운로드 필요**: 이 저장소는 대용량 데이터셋 파일을 포함하지 않습니다. 아래 [데이터셋 준비](#-데이터셋-준비) 섹션을 참조하세요.

## 📊 프로젝트 개요

### 해결하는 문제

**기존 문제점:**
- 수천~수만 개의 리뷰를 사람이 읽고 분석 → 시간/비용 과다
- 단순 통계(평균 별점)만으로는 실제 문제점 파악 불가
- 분석 후 "그래서 뭘 해야 하나?" → 실행 계획 부재

**제공하는 가치:**
- ✅ 자동화된 대규모 리뷰 분석
- ✅ 제품 측면별(배터리, 품질, 가격 등) 세부 분석 (ABSA)
- ✅ 경쟁사 비교를 통한 차별화 포인트 도출
- ✅ 우선순위가 명확한 실행 계획 자동 생성
- ✅ 경영진용 요약 보고서 + 시각화

## 📦 데이터셋 준비

이 저장소는 대용량 데이터셋 파일을 포함하지 않습니다. 사용하기 전에 데이터를 다운로드해야 합니다.

### Amazon Review Polarity Dataset (추천)

```bash
# datasets/ 디렉토리로 이동
cd datasets/

# 데이터셋 다운로드 (657 MB)
wget https://s3.amazonaws.com/fast-ai-nlp/amazon_review_polarity_csv.tgz

# 압축 해제
tar -xzf amazon_review_polarity_csv.tgz

# test.csv (168MB) 생성됨
```

### 데이터 형식

리뷰 데이터는 JSON Lines 형식이어야 합니다:

```json
{"overall": 5.0, "reviewText": "Great product!", "summary": "Excellent", "reviewTime": "01 1, 2024", "asin": "B001234567"}
{"overall": 2.0, "reviewText": "Not good", "summary": "Disappointed", "reviewTime": "01 2, 2024", "asin": "B001234567"}
```

## ✨ 주요 기능

- ✅ **대규모 리뷰 자동 분석**: 대량 리뷰 데이터 자동 처리
- ✅ **Aspect 기반 감성 분석 (ABSA)**: 제품 측면별 세부 분석
- ✅ **LLM 기반 인사이트**: 문제점 자동 도출 (gpt-oss:20b)
- ✅ **실행 계획 자동 생성**: 우선순위별 개선 계획
- ✅ **경쟁사 비교 분석**: 여러 제품 병렬 분석
- ✅ **시각화**: 4종 차트 자동 생성
- ✅ **성능 최적화**: LLM 캐싱, 병렬 처리
- ✅ **웹 인터페이스**: Streamlit 대시보드

## 🚀 빠른 시작

### 방법 1: 로컬 설치 + 웹 인터페이스

```bash
# 1. 가상 환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 패키지 설치 (웹 인터페이스 포함)
pip install -e ".[web]"

# 3. 웹 인터페이스 실행
streamlit run scripts/web_interface.py
# 브라우저: http://localhost:8501
```

### 방법 2: CLI 명령어

```bash
# 패키지 설치
pip install -e .

# 리뷰 분석 (데이터셋 다운로드 후 실행)
python scripts/run_analysis.py --data-path datasets/test.csv --limit 100
```

### Ollama 설정 (로컬 LLM)

```bash
# Ollama 설치 (https://ollama.com)

# gpt-oss:20b 모델 다운로드
ollama pull gpt-oss:20b

# Ollama 서버 실행
ollama serve
```

### 기본 분석 실행

```bash
python scripts/run_analysis.py \
    --data-path datasets/test.csv \
    --limit 100 \
    --output output/results.json
```

**결과**:
```
output/
├── results.json           # 전체 분석 결과
└── charts/                # 시각화 차트
    ├── rating_distribution.png
    ├── time_series.png
    ├── aspect_sentiment.png
    └── sentiment_pie.png
```

## 🏗️ 시스템 아키텍처

### Multi-Agent 구조

```
┌─────────────────────────────────────────────┐
│         CLI / Web Interface                 │
├─────────────────────────────────────────────┤
│         Orchestrator (조율자)                │
├─────────────────────────────────────────────┤
│         Agent Layer (6개 에이전트)           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Data    │  │Sentiment │  │ Insight  │  │
│  │Collection│  │ Analysis │  │Extraction│  │
│  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Action  │  │  Report  │  │Competitor│  │
│  │ Planning │  │Generation│  │ Analysis │  │
│  └──────────┘  └──────────┘  └──────────┘  │
├─────────────────────────────────────────────┤
│      Service Layer (공통 서비스)             │
│    LLM · Cache · Visualization              │
├─────────────────────────────────────────────┤
│      Data Layer (JSON/CSV Loaders)          │
└─────────────────────────────────────────────┘
```

### 데이터 플로우

```
리뷰 데이터 (JSON/CSV)
    ↓
[1] Data Collection → 전처리, 필터링
    ↓
[2] Sentiment Analysis → ABSA (측면별 감성)
    ↓
[3] Insight Extraction → LLM 기반 문제점 도출
    ↓
[4] Action Planning → 우선순위별 실행 계획
    ↓
[5] Report Generation → 시각화 + 경영진 요약
    ↓
결과 (JSON + PNG)
```

## 📁 프로젝트 구조

```
agentic_ai/
├── src/                     ⭐ 모든 소스 코드
│   ├── agents/              # 6개 비즈니스 에이전트
│   ├── core/                # 기본 프레임워크 (Orchestrator, BaseAgent)
│   ├── services/            # LLM, 캐시, 시각화 서비스
│   ├── utils/               # 로거, 에러 핸들러
│   ├── data/                # 데이터 로더 및 전처리
│   ├── prompts/             # LLM 프롬프트 템플릿 (Jinja2)
│   └── config/              # 설정 파일 (llm_config.yaml)
│
├── datasets/                # 데이터셋 (다운로드 필요)
├── scripts/                 # 실행 스크립트
│   ├── run_analysis.py      # CLI 분석 도구
│   └── web_interface.py     # Streamlit 웹 UI
│
├── README.md                # 프로젝트 문서
├── setup.py                 # Python 패키지 설정
└── requirements.txt         # 의존성 목록
```

## 📖 사용법

### Python

```python
from core.orchestrator import ReviewAnalysisOrchestrator

# Orchestrator 초기화
orchestrator = ReviewAnalysisOrchestrator(
    config={'output_dir': 'output'},
    llm_config_path='src/config/llm_config.yaml'
)

# 분석 실행 (데이터셋 다운로드 후 실행)
result = orchestrator.run_analysis(
    data_path='datasets/test.csv',
    product_id='B0123456',
    limit=500,
    enable_llm=True
)

# 결과 확인
print(f"Analyzed {result['basic_stats']['total_reviews']} reviews")
print(f"Average rating: {result['basic_stats']['avg_rating']}")
print(f"Sentiment: {result['sentiment_analysis']['sentiment_distribution']}")
```

### CLI 사용

```bash
# 기본 분석 (데이터셋 다운로드 후 실행)
python scripts/run_analysis.py --data-path datasets/test.csv --limit 1000

# 출력 경로 지정
python scripts/run_analysis.py \
    --data-path datasets/test.csv \
    --limit 500 \
    --output custom_output/results.json
```

## ⚡ 성능 최적화

시스템은 다음과 같은 최적화 기법을 사용합니다:

- **LLM 응답 캐싱**: 동일한 요청 재사용으로 응답 시간 단축
- **병렬 처리**: 경쟁사 비교 분석 시 최대 3개 제품 동시 분석 (ThreadPoolExecutor)
- **에러 처리**: Retry 로직 및 Rate limiting으로 안정성 향상
- **빠른 시각화**: Matplotlib/Seaborn 기반 차트 생성

## 🛠️ 기술 스택

### 언어 및 프레임워크

- **Python 3.9+**
- **pandas**: 데이터 분석
- **matplotlib/seaborn**: 시각화
- **Streamlit**: 웹 인터페이스
- **Ollama** (로컬): gpt-oss:20b

## 📝 라이선스

MIT License - 자유롭게 사용, 수정, 배포할 수 있습니다.

---

**Version**: 1.0.0
**Last Updated**: 2025-10-19
**Status**: Production Ready ✅
